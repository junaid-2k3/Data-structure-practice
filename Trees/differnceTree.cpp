// To check if a tree is a differnce tree or not 
// A difference tree is a tree for whose non-leaf nodes the differnce of sum of keys in its left subtrees is and right sub tree is equal to the key of the root node  
#include <iostream>
using namespace std;
struct Node {
    int data;
    Node* left;
    Node* right;

    Node(int val) {
        data = val;
        left = right = nullptr;
    }
};

bool IsDiffernce(Node* root){

}
int main(){

    return 0;
}
