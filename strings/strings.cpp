#include<iostream>
#include<string.h>
using namespace std;

int main(){
    ///string and character array act like the same things with differnct syntax 
    char str[]={'a','b','c'};
    string strr="abc";
    cout<<strr<<endl;
    cout<<str<<endl;
    cout<<strlen(str);
    cout<<endl;
    cout<<strlen(str);
    
}